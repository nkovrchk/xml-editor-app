export enum ESortValue {
    ASC = 'asc',
    DESC = 'desc',
}

export enum ESortBy {
    TITLE = 'title',
    CATEGORY = 'category',
    ID = 'id',
}

export enum EEditorAction {
    EDIT = 'EDIT',
    CREATE = 'CREATE',
}
